{
  "name": "qr-code-component-main",
  "version": "1.0.0",
  "description": "qr-code-component project from frontend masters",
  "main": "index.html",
  "scripts": {
    "start": "tailwindcss -i ./input.css -o ./style.css --watch ",
    "build": "mkdir -p dist && cp -r index.html images dist/ &&  tailwindcss -i ./input.css -o ./dist/style.css ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.4"
  }
}
